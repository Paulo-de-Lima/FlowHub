# URL da API FlowHub (servidor Express em server/)
# Web / iOS simulador: localhost funciona
# Android emulador: omita ou use http://10.0.2.2:3000 (fallback automático no app)
# Celular físico: use o IP da máquina na mesma rede Wi‑Fi
EXPO_PUBLIC_API_URL=http://localhost:3000
